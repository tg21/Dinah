A multi agent system caled DND.
We can access and manage that system from web (localhost).

read more details in pdf_transcribe.txt.
add notes from that pdf to this requirements.md. 
not all agents are mentioned in pdf file read agent_base.md for full list.

we'll use pixiejs for our frontend.

at backend we can have a nodejs backend its fine. we'll support opencode, claudecode, codex and gemini-cli harnesses for now. so we should be able to start agents in those systems, and have their output in our system.
