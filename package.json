{
  "name": "dnd",
  "version": "1.0.0",
  "description": "",
  "main": "src/backend.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && mkdir dist && cp src/backend.js dist/ && cp src/harnessRegistry.js dist/ && cp src/frontend.html dist/",
    "start": "node src/backend.js"
  },
  "keywords": [],
  "type": "module",
  "dependencies": {
    "body-parser": "^2.3.0",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}